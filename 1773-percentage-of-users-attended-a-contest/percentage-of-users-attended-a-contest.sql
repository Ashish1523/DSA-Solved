# Write your MySQL query statement below
select contest_id,(round(count(contest_id)/(select count(user_id) from users),4)*100) as percentage from users u join register r where u.user_id=r.user_id group by contest_id order by percentage desc,contest_id 